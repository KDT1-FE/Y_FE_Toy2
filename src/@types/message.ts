export interface MessageData {
  createdAt: string;
  id: string;
  text: string;
  userId: string;
}
export interface Message {
  name: string;
  id: string;
  picture: string;
  createdAt: string;
  text: string;
}
