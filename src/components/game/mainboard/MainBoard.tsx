import React from 'react';
import styled from 'styled-components';

const MainWrapper = styled.div`
  flex: 2;
`;

export default function MainBoard() {
  return <MainWrapper>MainBoard</MainWrapper>;
}
