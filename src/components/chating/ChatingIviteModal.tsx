'use client';

import styled, { keyframes } from 'styled-components';

interface User {
    username: string;
    id: string;
    picture: string;
}

//type
export default function ChatingInviteModal(props: any) {
    return <></>;
}
