import React from 'react';

// components
import LoginForm from '@/components/Login/LoginForm';

const page = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default page;
