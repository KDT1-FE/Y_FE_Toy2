import React from 'react';

// components
import RegisterForm from '@/components/Register/RegisterForm';

const page = () => {
    return (
        <div>
            <RegisterForm />
        </div>
    );
};

export default page;
