'use client';

import React, { useState } from 'react';
import ChatPage from '@/components/chats/ChatPage';

const MyChats = () => {
    return <ChatPage userType="my" />;
};

export default MyChats;
